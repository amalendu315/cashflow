@model LedgerMasterFormViewModel

@{
    ViewData["Title"] = "Add Ledger";
}

<div class="d-flex justify-content-between align-items-center mb-4">
    <div>
        <h1 class="h3 mb-1">Add Ledger</h1>
        <p class="text-muted mb-0">Create a bank account or cash pool for payment approvals.</p>
    </div>

    <a asp-action="Index" class="btn btn-outline-secondary">
        Back to List
    </a>
</div>

<div class="card shadow-sm">
    <div class="card-body">
        <form asp-action="Create" method="post" asp-antiforgery="true">
            <partial name="_LedgerMasterFields" model="Model" />

            <div class="d-flex gap-2">
                <button type="submit" class="btn btn-primary">
                    Save Ledger
                </button>

                <a asp-action="Index" class="btn btn-outline-secondary">
                    Cancel
                </a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}